# 升级指南（UPGRADING）

> **每次 core 发版都必须在本目录写一份升级文档**（`docs/upgrades/<版本>.md`），
> 说明改动清单与**破坏性改动（BREAKING）**的处理方式；同时更新根目录 `CHANGELOG.md`
> 并在下方索引登记。版本号遵循 `主.次.修订`：**破坏性改动必须升 `次` 号**（0.x 阶段升次号、
> 1.0 之后升主号）。

## 版本索引（新 → 旧）

| 版本 | 日期 | 摘要 | 破坏性？ |
|---|---|---|---|
| [0.2.1](upgrades/0.2.1.md) | 2026-09-06 | 端口记忆（稳定端口） | 无破坏（默认开，行为注意） |
| [0.2.0](upgrades/0.2.0.md) | 2026-09-06 | 共享核多站点（host 模式） | 无破坏 |
| [0.1.1](upgrades/0.1.1.md) | 2026-09-06 | 运行期 root ctx / 端口避让 / 控制台日志 | **有（默认端口变更）** |
| [0.1.0](upgrades/0.1.0.md) | 2026-09-06 | 首个版本 | — |

## 升级步骤（常规）

1. 阅读本索引 → 打开**目标版本**及各中间版本升级文档，确认破坏性改动
2. 更新 core 引用：
   - 本地路径（file:）：目录已是最新，无需动作
   - GitHub 依赖：改 package.json 锁定的 tag/commit 后 `npm install`
3. 按升级文档执行需要的手动操作（如有），跑 core 自检确认：
   ```bash
   npm run self-test        # main 路径自检
   npm run self-test-cordis # cordis 装配链路自检
   ```
4. 重启你的站点（端口记忆会钉住原端口，见 0.2.1）